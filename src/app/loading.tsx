export default function Loading() {
  return (
    <main className="mx-auto w-full max-w-3xl p-6">
      <div className="animate-pulse rounded-xl border p-6 text-sm text-muted-foreground">PACKLOG 行前志 正在加载...</div>
    </main>
  );
}
