# PACKLOG V2 功能清单（持续迭代版）

> 用法：每次评审/开发后更新状态。  
> 状态建议：`todo` / `doing` / `done` / `blocked`。

## 0. 核心目标（你的长期方向）

- 以“创建行程 -> 复用装备 -> 快速打包检查”为主链路
- 社区作为高质量补充，不是主入口
- 装备级评价/标签/真实场景沉淀，支持长期决策

---

## 1. 架构与数据（V2 主线）

- [ ] `todo` 新增 `gear_master` 主表（全局唯一装备实体）
- [ ] `todo` `gear_locker` 增 `gear_id` 并建立 `(user_id, gear_id)` 唯一关系
- [ ] `todo` `trip_items` 增 `gear_id`（保留 `source_locker_id` 兼容一段周期）
- [ ] `todo` `community_template_items` 增 `gear_id`
- [ ] `todo` 建立匹配回填脚本（名称归一 + 品牌 + 分类）
- [ ] `todo` 回填 QC 报告（匹配率/冲突率/低置信条目）
- [ ] `todo` 统一统计口径为 `gear_id` 聚合

---

## 2. 主流程体验（优先级最高）

- [ ] `todo` 行程页：新增条目默认置顶/排序稳定，拖拽一致性再校验
- [ ] `todo` 行程页：批量操作反馈继续提速（状态、容器、保存仓库）
- [ ] `todo` 仓库导入：支持“按场景模板一键导入 + 差异增减”
- [ ] `todo` 仓库页：按“常用套装（露营/音乐节/山地）”批量导入行程
- [ ] `todo` 打包流程：待购/再说/已打包切换保持可逆且可追踪

---

## 3. 社区能力（服务于主流程）

- [ ] `todo` 相似场景推荐：优先与你当前行程标签接近的清单
- [ ] `todo` 社区条目导入时显式展示“是否命中你仓库同款”
- [ ] `todo` 清单/单品引用次数改为 gear 级可信统计
- [ ] `todo` 社区清单质量分层（高质量来源优先）

---

## 4. 装备知识层（你提到的后续重点）

- [ ] `todo` 单装备评价模型（评分、优缺点、适用场景）
- [ ] `todo` 真实使用标签（雨天/高海拔/音乐节/长途飞行等）
- [ ] `todo` 版本化备注（同装备在不同行程的变化记录）
- [ ] `todo` “被谁在什么场景高频使用”可视化

---

## 5. 性能与设计（当前痛点）

- [ ] `todo` 行程首页操作延迟优化（置顶/归档/删除）
- [ ] `todo` 移动端交互稳定性（输入缩放、按钮触控区、底部条漂移）
- [ ] `todo` 页面视觉层次增强（减少“扁平感”，保持简洁高级）
- [ ] `todo` 大列表渲染优化（虚拟化/分段加载）

---

## 6. 每次大更新前的检查单

- [ ] `todo` 迁移脚本已在本地 + 远端演练
- [ ] `todo` 关键路径回归：创建行程 / 导入仓库 / 社区复制 / 打包勾选
- [ ] `todo` 多语一致性检查（zh-CN / zh-TW / en）
- [ ] `todo` 指标口径验证（copy_count / item_add_count / usage）
- [ ] `todo` 失败回滚预案可执行

---

## 7. 迭代记录（追加区）

### 2026-04-23（初始化）

- 建立 V2 主线：`gear_master` 引用化架构
- 输出全局影响审计：`docs/roadmap/V2_GEAR_MASTER_IMPACT_AUDIT.md`
- 后续所有大更新按本清单推进并持续改状态

